/**
  *
  * Beschreibung
  *
  * @version 1.0 vom 02.02.2017
  * @author 
  */


import java.util.*;

public class Burrrrrshi {
  
  public static void main(String[] alshabaghitihualakbar) {
    
    int[] a = {1,4,5,8,9,6,3,12,14,18,33};
    System.out.println(Arrays.toString(a));
    
    
    
    System.out.println(Arrays.toString(links));
    System.out.println(Arrays.toString(rechts));
    
    
  } // end of main
  
  
  
  
  
  
  public static int[] MergeSort (int[] ) {
    
    //Array "a" in links und rechts aufteielen
    
    int la = a.length / 2;
    
    int[] links = new int[la];
    int[] rechts = new int[a.length-la];
    
    //Links befuellen
    for (int i = 0; i<links.length ; i++ ) {
      links[i] = a[i];
    }
    
    //Rechts befuellen
    for (int j = 0; j<rechts.length ; j++ ) {
      rechts[j] = a[la];
      la++;
      
    } // end of for
    //Ende aufteilen
    
    
    
    if (a.length == 1) {
      return a;  
    } else {
      
    } // end of if-else
    
    
    
    
  }
  
} // end of class Burrrrrshi
